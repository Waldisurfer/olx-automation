[build]
  builder  = "NIXPACKS"
  buildCommand = "npm install"

[deploy]
  startCommand = "npm run build && node --experimental-sqlite dist/main.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3

[healthcheck]
  httpPath = "/api/health"
  timeoutSecs = 30
