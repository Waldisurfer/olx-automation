[build]
  command    = "npm run build"
  publish    = "dist"

[build.environment]
  NODE_VERSION = "22"
  VITE_API_BASE_URL = "https://backend-production-0723.up.railway.app"

# SPA — wszystkie ścieżki do index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
